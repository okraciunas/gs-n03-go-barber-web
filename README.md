# gs-n03-go-barber-web

## Este projeto utiliza:

- `create-react-app` para inicialização e criação do setup inicial do projeto;
- `React` + `TypeScript`;
- Formatação de código com `EditorConfig`, `ESLint` e `Prettier`;
